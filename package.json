{
  "name": "monorepo-starter",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "private": true,
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "build": "bun run build",
    "sb": "bun run --cwd apps/design-system sb",
    "bsb": "bun run --cwd apps/design-system bsb"
  }
}
